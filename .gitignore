# Plugin-Binaries (Wegwerf-Beispiele — kommen nur als ZIPs in die Releases)
/plugins/*/plugin
/plugins/*/plugin.exe
/plugins/*/*.exe

# Signierte Plugin-ZIPs + lokale Release-Assets — liegen nur in GitHub Releases
*.glorious-plugin
/release-assets/

# Signing-Key — NIE committen!
key.pem
*.pem
